# tinystats

A tiny command-line tool that prints the arithmetic mean of the numbers it is
given, plus helpers for parsing ports. Correct arithmetic is the whole job:
`python cli.py 2 4 6` must print `4.0`.
